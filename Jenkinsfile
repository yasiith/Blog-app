pipeline {
    agent any

    stages {
        stage('Checkout Code') {
            steps {
                git 'https://github.com/yasiith/Blog-app.git'  // Checkout the code from GitHub
            }
        }

    }
}
